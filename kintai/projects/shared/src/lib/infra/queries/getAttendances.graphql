query GetAttendances($from: String!, $to: String!, $employeeId: ID) {
  attendances(from: $from, to: $to, employeeId: $employeeId) {
    id
    employeeId
    date
    holiday
    status
    free_note
    start
    end
    work
    rest
    overwork
    nighttime
    created
    updated
  }
}
